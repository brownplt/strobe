class HTMLWindow prototype {} {
  document : { },
  setTimeout : (-> Void) * Int -> Int
}

class String prototype {} {
  charCodeAt : Int -> Double
}

document : { }
window : HTMLWindow
setTimeout : (-> Void) * Int -> Int
undefined: Void

parseInt : String + Number * Int + Void -> Int
parseFloat : String + Number -> Double

gddSoundStateStopped : Int
gddSoundStatePlaying : Int

class Audioclip prototype {} {
  balance : Int,
  currentPosition : Int,
  duration : Int,
  error: Int,
  src : String,
  state : Int,
  volume : Int
}

framework : {
  openURL : String -> Void,
  audio : {
    play : String * (Dom * Int -> Void) -> Audioclip
  }
}
